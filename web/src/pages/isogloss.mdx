---
title: Ninety miles up the lake from Chicago, the word changes.
description: An isogloss is the line where one word gives way to another, and the lines that matter run through states rather than between them.
layout: ../layouts/Base.astro
---

import Isogloss from "../sections/Isogloss";

{/*
  The plate that shows what the recovered geography is for.

  It sits between the recovery and the pages that turn inward on the model,
  because it is the last place the page is about the country rather than
  about the method.

  The opening prose is Markdown so the page has a heading and a contents
  rail. The closing paragraph stays inside the island because it quotes the
  widest and tightest boundaries, which are computed from the data there.
*/}

An **isogloss** is the line where one word gives way to another. It is the oldest object in dialect geography and the reason anybody wanted the survey's sub-state detail in the first place, because the lines that matter run *through* states rather than between them. No table of state percentages can put a boundary between Milwaukee and Chicago; a surface can.

## Drawing one takes two words, not one

A single answer's map is a heat map: it says where a word is common, which is mostly a picture of where people live. Subtracting one recovered surface from the other gives the odds of the first word against the second at every point in the country, and the contour where those odds are even is the isogloss.

<Isogloss client:visible />
